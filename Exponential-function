#include <stdio.h>
#include <stdlib.h>

int power(int x , int y){
    long long int val=1;
    for(int i=0; i<y; i++){
        val=val*x;
    }
    return val;
}

int main(int argc, char const *argv[])
{
    int a,b;
    a=13;
    b=12;
    printf("The power of %d is : %lld\n",a,power(a,b));
    /* code */
    return 0;
}

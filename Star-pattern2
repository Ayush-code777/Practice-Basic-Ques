#include <stdio.h>
#include <stdlib.h>


int main(int argc, char const *argv[])
{
    int n = 10;
    
    // Run this for loop n times
    for(int i=0 ; i<n ; i++){
       
       for(int j=0; j<i+1; j++){
        printf(" * ");
       }
       printf("\n");
    }
    
    return 0;
}
